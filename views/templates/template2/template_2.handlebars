{{!-- {{> header}} --}}

<main>
    <h2>Welcome to the Template2 Page!</h2>
    <p>This is the Template2 content.</p>
    <a href="/template2/about">About</a>
    <ul>
        
        {{#each data}}
            <li>Name: {{this.name}}</li>
            <li>Username: {{this.username}}</li>
            <li>Email: {{this.email}}</li>
        {{/each}}
    </ul>
       
</main>
